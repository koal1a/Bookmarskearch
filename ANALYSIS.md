# 프로젝트 분석: USB (Universal Search Bookmarker)

## 1. 프로젝트 개요

이 프로젝트는 PyQt5로 제작된 데스크톱 애플리케이션으로, 사용자가 즐겨찾는 검색어와 여러 검색 엔진 URL을 효율적으로 관리하고 이를 통해 신속하게 웹 검색을 수행할 수 있도록 돕는 검색 유틸리티입니다.

애플리케이션은 크게 세 개의 패널로 구성된 UI를 가지며, 다크 모드 테마와 프로필 관리 등 사용자 편의를 위한 다양한 기능을 제공합니다.

## 2. 아키텍처

프로젝트는 역할에 따라 명확하게 세 부분으로 나뉘어 있습니다:

### `main.py`
- 애플리케이션의 **진입점(Entry Point)**입니다.
- `PyQt5` 애플리케이션 인스턴스를 생성하고, `AppState`를 초기화하며, 메인 윈도우(`MainWindow`)를 생성하고 실행합니다.
- 시작 시 `core.settings.set_work_directory()`를 호출하여 파일 경로의 기준이 되는 작업 디렉토리를 설정하는 중요한 역할을 합니다.

### `core/` 디렉토리 (핵심 로직)
애플리케이션의 데이터와 비즈니스 로직을 담당합니다. UI와 독립적으로 설계되어 있습니다.

- **`state.py`**:
  - `AppState` 클래스는 애플리케이션의 모든 상태(데이터)를 메모리에 담는 **중앙 저장소(Single Source of Truth)** 역할을 합니다.
  - 검색 기록, 저장된 URL, 태그, 그룹, UI 설정 등 모든 데이터를 속성으로 가집니다.
  - `core.settings` 모듈을 사용하여 데이터를 JSON 파일로 로드하고 저장하는 책임을 가집니다.

- **`settings.py`**:
  - 모든 **파일 입출력(I/O)**을 담당합니다.
  - `app_config.json` (마지막 프로필, 폰트 크기 등)과 `settings_{profile_name}.json` (프로필별 상세 데이터) 두 종류의 설정 파일을 관리합니다.
  - 이전 버전과의 호환성을 위한 데이터 마이그레이션 로직도 포함합니다.

- **`actions.py`**:
  - 사용자의 행동(Action)에 해당하는 **실제 기능들을 함수로 정의**합니다. (예: 웹 브라우저 열기, 데이터 추가/삭제/수정).
  - 이 함수들은 UI 레이어에서 호출되며, `AppState` 객체를 인자로 받아 상태를 변경하고 저장을 요청합니다.

### `ui/` 디렉토리 (사용자 인터페이스)
PyQt5 위젯으로 구성된 모든 UI 코드를 포함합니다.

- **`main_window.py`**:
  - `QMainWindow`를 상속받는 메인 윈도우입니다.
  - `QSplitter`를 사용하여 세 개의 주 패널(`LeftPanel`, `CenterPanel`, `RightPanel`)을 배치합니다.
  - 메뉴 바, 프로필 전환, 테마 변경, 창 상태 저장 등 전역적인 UI 관리를 담당합니다.

- **패널 (`left_panel.py`, `center_panel.py`, `right_panel.py`)**:
  - 각각의 패널은 UI의 한 구역을 책임집니다.
  - `AppState`를 주입받아 데이터를 화면에 그리고, 사용자 입력(버튼 클릭, 텍스트 입력 등)을 받아 `core.actions`의 함수를 호출합니다.
  - 패널들은 서로 독립적이지만, 필요시 `main_window`를 통해 다른 패널의 `update` 메소드를 호출하여 UI를 동기화합니다. (예: 왼쪽 패널에서 북마크 추가 시 중앙 패널 새로고침)

- **`dialogs.py`**:
  - 데이터 추가/수정을 위한 모든 **대화 상자(Dialog)**를 정의합니다. (예: 검색어 편집, URL 수정, 그룹 관리).
  - 복잡한 입력 처리를 패널로부터 분리하여 코드의 관심사를 명확하게 나눕니다.

- **`widgets.py`**:
  - `ClearOnFocusLineEdit`와 같은 커스텀 위젯과 애플리케이션의 다크 테마를 정의하는 **QSS 스타일시트**를 포함합니다.

## 3. 데이터 흐름

1.  **시작**: `main.py`가 `AppState`를 생성합니다.
2.  **로드**: `AppState`는 `core.settings`를 통해 `app_config.json`에서 마지막으로 사용한 프로필 이름을 읽고, 해당 `settings_{profile_name}.json` 파일에서 모든 데이터를 로드하여 자신의 속성을 채웁니다.
3.  **UI 렌더링**: `AppState` 객체는 `MainWindow`를 통해 모든 하위 UI 컴포넌트(패널, 다이얼로그 등)에 전달(주입)됩니다. 각 UI 컴포넌트는 `AppState`의 데이터를 읽어 화면을 그립니다.
4.  **사용자 행동**: 사용자가 UI와 상호작용합니다 (예: 검색 버튼 클릭).
5.  **액션 호출**: UI 컴포넌트는 `core.actions`에 정의된 적절한 함수를 호출하고, `AppState`와 사용자 입력값을 전달합니다.
6.  **상태 변경**: `actions` 함수는 전달받은 `AppState`의 데이터를 변경합니다.
7.  **저장**: `actions` 함수는 `AppState`의 `save_*` 메소드를 호출하여 변경된 내용을 JSON 파일에 즉시 또는 주기적으로 저장합니다.
8.  **UI 업데이트**: 상태 변경 후, UI 컴포넌트는 자신의 `update_*` 메소드를 호출하여 `AppState`로부터 최신 데이터를 다시 읽어 화면을 새로 고칩니다.
9.  **종료**: 사용자가 앱을 닫을 때, `MainWindow`의 `closeEvent`가 트리거되어 최종 UI 상태(창 크기, 스플리터 위치 등)와 모든 데이터를 `AppState`를 통해 파일에 저장합니다.

## 4. 주요 기능 및 특징

-   **프로필 시스템**: 여러 사용자의 설정을 `profile1`, `profile2` 등으로 분리하여 관리할 수 있습니다.
-   **즐겨찾기 관리**: 검색어를 그룹과 태그로 분류하여 `QTreeWidget`에 체계적으로 관리합니다.
-   **URL 관리 및 선택적 검색**: 검색에 사용할 검색 엔진 URL을 관리하며, 체크박스를 통해 검색에 포함할 사이트를 동적으로 선택할 수 있습니다.
-   **프리셋 (Preset)**: 자주 사용하는 URL 체크 조합을 최대 4개까지 저장하고 빠르게 불러올 수 있는 워크플로우를 제공합니다. (오른쪽 패널)
-   **고급 편집 기능**: 각 패널의 데이터(검색어, URL 등)를 전용 대화상자뿐만 아니라, JSON이나 특정 형식의 텍스트로 직접 편집하는 기능을 제공합니다.
-   **상태 저장**: 창의 크기와 위치, 패널 구분선의 위치, 트리의 확장/축소 상태, 컬럼 너비 등 거의 모든 UI 상태가 저장되어 다음 실행 시 복원됩니다.
-   **테마 지원**: 기본 테마와 다크 테마를 전환할 수 있으며, 폰트 크기도 조절 가능합니다.
